package majhrs16.dl;

// import java.awt.Dimension;
// import java.awt.Toolkit;

public class util {
	/*// INFUNCIONAL
    public static int[] getConsoleSize() {
        Dimension screenSize = Toolkit.getDefaultToolkit().getScreenSize();

        int consoleWidthInCharacters =  (int) screenSize.getWidth() / 8;
        int consoleHeightInCharacters = (int) screenSize.getHeight() / 16;

        return new int[]{consoleWidthInCharacters, consoleHeightInCharacters};
    }
    */
}
